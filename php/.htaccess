# Configuración para Apache
# Permite que index.php sea el archivo por defecto

DirectoryIndex index.php index.html

# Habilitar reescritura de URLs (si es necesario en el futuro)
# RewriteEngine On
# RewriteBase /SorteosWeb-conLoDeJorge/SorteosWeb-main/php/

# Mostrar errores de PHP (solo en desarrollo)
# En producción, comentar estas líneas
php_flag display_errors On
php_value error_reporting E_ALL

# Configuración de charset
AddDefaultCharset UTF-8

# Proteger archivos sensibles
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|sql)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
